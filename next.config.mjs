/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // The UI is JS/JSX with inline styles + one global stylesheet — nothing exotic.
  // Add `images: { remotePatterns: [...] }` here if you later load remote images
  // through next/image.
};

export default nextConfig;
